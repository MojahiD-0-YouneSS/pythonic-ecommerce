volumes:
  pythonic_ecommerce_local_postgres_data: {}
  pythonic_ecommerce_local_postgres_data_backups: {}
  pythonic_ecommerce_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./devops/compose/local/django/Dockerfile
    image: pythonic_ecommerce_local_django
    container_name: pythonic_ecommerce_local_django
    depends_on:
      - postgres
      - redis
      - mailpit
    volumes:
      - .:/app:z
    env_file:
      - ./devops/.envs/.local/.django
      - ./devops/.envs/.local/.postgres
    ports:
      - '8000:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./devops/compose/production/postgres/Dockerfile
    image: pythonic_ecommerce_production_postgres
    container_name: pythonic_ecommerce_local_postgres
    volumes:
      - pythonic_ecommerce_local_postgres_data:/var/lib/postgresql/data
      - pythonic_ecommerce_local_postgres_data_backups:/backups
    env_file:
      - ./devops/.envs/.local/.postgres

  mailpit:
    image: docker.io/axllent/mailpit:latest
    container_name: pythonic_ecommerce_local_mailpit
    ports:
      - "8025:8025"

  redis:
    image: docker.io/redis:6
    container_name: pythonic_ecommerce_local_redis
    
    volumes:
      - pythonic_ecommerce_local_redis_data:/data
    

  celeryworker:
    <<: *django
    image: pythonic_ecommerce_local_celeryworker
    container_name: pythonic_ecommerce_local_celeryworker
    depends_on:
      - redis
      - postgres
      - mailpit
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: pythonic_ecommerce_local_celerybeat
    container_name: pythonic_ecommerce_local_celerybeat
    depends_on:
      - redis
      - postgres
      - mailpit
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: pythonic_ecommerce_local_flower
    container_name: pythonic_ecommerce_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
